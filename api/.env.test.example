# Basis-Variablen für lokale E2E-/Integrationstests
NODE_ENV=test

# Separate Test-Datenbank (lokal oder aus Docker)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/eventos2_test

# Standardmäßig deaktiviert für Production-Sicherheit
# Auf 'true' setzen, damit die Test-Helper-Endpoints verfügbar sind
ENABLE_TEST_ENDPOINTS=true

# JWT Secret nur für Tests (in Production unbedingt überschreiben!)
JWT_SECRET=jwt-test-secret

# Default-Credentials für automatische Admin-Erstellung
ADMIN_EMAIL=admin@example.dev
ADMIN_NICK=admin
ADMIN_PASSWORD=changeMe

# Default-Credentials für vorbereiteten Testnutzer
TEST_USER_EMAIL=testuser@example.dev
TEST_USER_PASSWORD=changeMe
