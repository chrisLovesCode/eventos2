# ============================================
# PRODUCTION Environment Configuration
# ============================================
# ⚠️  Copy this file to .env.prod and fill in your actual production values
# ⚠️  NEVER commit .env.prod to git!

# Database Configuration
DATABASE_URL=postgresql://eventos2_user:CHANGE_THIS_STRONG_PASSWORD@postgres:5432/eventos2_db
POSTGRES_DB=eventos2_db
POSTGRES_USER=eventos2_user
POSTGRES_PASSWORD=CHANGE_THIS_STRONG_PASSWORD

# Node Environment
NODE_ENV=production

# API Configuration
PORT=3000
# ✅ Internal container-to-container communication
API_URL=http://api:3000
# ✅ Public API URL (through nginx)
API_URL_HOST=https://your-domain.com/api
API_PUBLIC_URL=https://your-domain.com/api

# Next.js Configuration
# ⚠️ NEXT_PUBLIC_* is exposed to the browser!
NEXT_PUBLIC_API_URL=https://your-domain.com/api

# JWT Secret
# ⚠️ Generate a strong secret: openssl rand -base64 32
JWT_SECRET=CHANGE_THIS_TO_A_STRONG_RANDOM_SECRET_AT_LEAST_32_CHARS
REFRESH_TOKEN_SECRET=CHANGE_THIS_TO_A_STRONG_RANDOM_SECRET_AT_LEAST_32_CHARS

# CORS Origins (comma separated)
# Include your actual domain(s)
CORS_ORIGINS=https://your-domain.com,https://www.your-domain.com

# Enable admin-only E2E endpoints in production (email verify cleanup etc.)
# Default: off. Set to true if you want to run Playwright E2E flows against production.
ENABLE_TEST_ENDPOINTS=false

# Geocoding Service (Geoapify)
# Get your API key from: https://www.geoapify.com/
GEOAPIFY_API_KEY=your-geoapify-api-key

# Admin User Configuration
ADMIN_EMAIL=admin@your-domain.com
ADMIN_NICK=admin
ADMIN_PASSWORD=CHANGE_THIS_SECURE_ADMIN_PASSWORD

# Test User Configuration (optional in production)
TEST_USER_EMAIL=testuser@your-domain.com
TEST_USER_NICK=testuser
TEST_USER_PASSWORD=CHANGE_THIS_TEST_PASSWORD

# ============================================
# Email Configuration (SMTP)
# ============================================
# Production SMTP Settings - Example with Gmail
# For Gmail: Enable 2FA and create an App Password
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-gmail-app-password
SMTP_FROM=noreply@your-domain.com

# Alternative: SendGrid
# SMTP_HOST=smtp.sendgrid.net
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=apikey
# SMTP_PASSWORD=your-sendgrid-api-key
# SMTP_FROM=noreply@your-domain.com

# Alternative: AWS SES
# SMTP_HOST=email-smtp.eu-central-1.amazonaws.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=your-ses-smtp-user
# SMTP_PASSWORD=your-ses-smtp-password
# SMTP_FROM=noreply@your-domain.com

# Frontend URL for email links
FRONTEND_URL=https://your-domain.com

# ============================================
# OAuth Providers Configuration (Optional)
# ============================================

# Google OAuth 2.0
GOOGLE_CLIENT_ID=your-production-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=GOCSPX-your-production-google-client-secret

# Facebook OAuth
FACEBOOK_APP_ID=your-production-facebook-app-id
FACEBOOK_APP_SECRET=your-production-facebook-app-secret

# Apple Sign In
APPLE_CLIENT_ID=com.your-domain.signin
APPLE_TEAM_ID=your-apple-team-id
APPLE_KEY_ID=your-apple-key-id
APPLE_PRIVATE_KEY_PATH=./certs/apple-private-key.p8

# ============================================
# Server Configuration (for deployment script)
# ============================================
# These are used by deploy.sh script
PROD_SERVER_HOST=123.456.789.101
PROD_SERVER_USER=root
PROD_SERVER_PORT=22
PROD_DEPLOY_PATH=/opt/eventos2
